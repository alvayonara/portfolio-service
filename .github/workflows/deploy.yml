name: Deploy portfolio-service to Cloud Run

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4
      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Configure Docker
        run: gcloud auth configure-docker asia-southeast1-docker.pkg.dev --quiet

      - name: Build Docker image
        run: |
          docker build -t asia-southeast1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/backend/portfolio-backend:latest .

      - name: Push Docker image
        run: |
          docker push asia-southeast1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/backend/portfolio-backend:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy portfolio-service \
            --image asia-southeast1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/backend/portfolio-backend:latest \
            --region asia-southeast1 \
            --platform managed \
            --allow-unauthenticated \
            --service-account portfolio-backend@portfolio-service-485616.iam.gserviceaccount.com \
            --set-env-vars "SPRING_R2DBC_URL=${{ secrets.SPRING_R2DBC_URL }}" \
            --set-env-vars "SPRING_R2DBC_USERNAME=${{ secrets.SPRING_R2DBC_USERNAME }}" \
            --set-env-vars "SPRING_R2DBC_PASSWORD=${{ secrets.SPRING_R2DBC_PASSWORD }}" \
            --set-env-vars "REDIS_HOST=${{ secrets.REDIS_HOST }}" \
            --set-env-vars "REDIS_PORT=${{ secrets.REDIS_PORT }}" \
            --set-env-vars "REDIS_USERNAME=${{ secrets.REDIS_USERNAME }}" \
            --set-env-vars "REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }}" \
            --set-env-vars "MAIL_HOST=${{ secrets.MAIL_HOST }}" \
            --set-env-vars "MAIL_PORT=${{ secrets.MAIL_PORT }}" \
            --set-env-vars "MAIL_USERNAME=${{ secrets.MAIL_USERNAME }}" \
            --set-env-vars "MAIL_PASSWORD=${{ secrets.MAIL_PASSWORD }}" \
            --set-env-vars "JWT_KEY_ID=${{ secrets.JWT_KEY_ID }}" \
            --set-env-vars "JWT_SECRET=${{ secrets.JWT_SECRET }}" \
            --set-env-vars "AWS_REGION_NAME=${{ secrets.AWS_REGION_NAME }}" \
            --set-env-vars "S3_BUCKET=${{ secrets.S3_BUCKET }}" \
            --set-env-vars "S3_ACCESS_KEY=${{ secrets.S3_ACCESS_KEY }}" \
            --set-env-vars "S3_SECRET_KEY=${{ secrets.S3_SECRET_KEY }}" \
            --set-env-vars "S3_RESUME_PREFIX=${{ secrets.S3_RESUME_PREFIX }}" \
            --set-env-vars "S3_PROJECT_PREFIX=${{ secrets.S3_PROJECT_PREFIX }}" \
            --set-env-vars "S3_PROFILE_PREFIX=${{ secrets.S3_PROFILE_PREFIX }}" \
            --set-env-vars "CORS_ALLOWED_ORIGINS=${{ secrets.CORS_ALLOWED_ORIGINS }}"
